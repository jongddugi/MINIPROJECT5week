# 진행 가이드 — 8단계 코칭 체크리스트

이 문서는 각 단계에서 "무엇을, 왜, 어떤 자료를 보고" 하면 되는지 안내하는 문서입니다.
**정답 코드는 담지 않습니다** — 직접 판단하고 작성하는 것이 목표이기 때문입니다.
막히면 에러 메시지나 작성한 코드를 보여주면, 코드를 대신 써주는 대신 질문·힌트로 같이 풀어갑니다.

각 STEP은 `PS` 폴더 안의 강의 자료를 참고 자료로 연결해 두었습니다 (전부 읽기 전용 참고용).

---

## STEP 0. 환경 준비

- [ ] 이 프로젝트만을 위한 새 폴더 생성 (PS 폴더 밖 또는 PS 안의 새 하위폴더 — 본인이 정하기)
- [ ] 가상환경 생성 + 활성화
- [ ] 필요 라이브러리 목록 잡기: `requests`, `beautifulsoup4`, `pandas`, `chromadb`, Gemini SDK, `python-dotenv`, `streamlit` 등 — Step이 진행되며 필요한 걸 그때그때 추가해도 됨
- [ ] Gemini API 키 발급 후 `.env`에 저장 (커밋 금지)

**완료 기준:** 가상환경 활성화 상태에서 주요 라이브러리 import 성공, `.env` 값 로드 확인

---

## STEP 1. Crawling + 데이터 문서

**목표:** 서울시 정책 상세 페이지 + 자치구 정책 목록 + 공지사항에서 정책 50~150건 원본 수집

**먼저 확인할 것**
- [ ] `robots.txt` 확인
- [ ] 브라우저 개발자도구(F12) → Network 탭에서 목록 페이지가 정적 HTML인지, 아니면 API를 따로 호출하는 방식인지 확인 (이 사이트는 목록에 페이지네이션/필터가 있어서 API 호출 방식일 가능성도 있음 — 직접 확인 필요)
- [ ] 상세 페이지 URL 패턴에서 정책 ID(`plcyBizId`)를 어떻게 뽑을지 확인

**절차 제안**
1. 목록 페이지(자치구 목록, 공지사항)에서 정책/공고 링크 수집
2. 상세 페이지 HTML을 먼저 파일로 저장 → 저장된 파일을 대상으로 파싱 (코드 고칠 때마다 재요청하지 않기 위함)
3. 정책 유형 / 기관 / 지원 내용 / 신청 기간 / 신청 자격 / 신청 절차를 각각 뽑아내기
4. 예외 케이스 방어: 특정 항목이 없는 정책, 상시모집이라 종료일이 없는 정책 등

**생각해볼 질문**
- 할인율처럼 "없을 수도 있는" 항목이 이 사이트에도 있는가? (`select_one()` 결과가 `None`인 경우)
- 자치구 정책과 서울시 정책을 같은 스키마로 담을 수 있는가, 아니면 필드가 다른가?
- 신청 기간이 "상시모집"인 경우 시작일/종료일을 어떻게 표현할 것인가?

**참고 자료 (읽기 전용)**
- `수업자료/data_lec3/day3/static_crawler.py` — 정적 페이지 요청/헤더/저장 패턴
- `수업자료/data_lec3/quiz2/quiz_local_event_crawling.py`, `crawling_quiz.txt` — 카드형 리스트 파싱, URL 쿼리 파라미터 추출 (이번 프로젝트와 구조가 비슷한 연습문제)
- `mini3/LS_MINI_3/docs/CRAWLING_PLAN.md` — 다른 프로젝트(Steam)지만 "원본 저장 후 파싱 분리", "예외 케이스 목록화" 절차는 그대로 적용 가능

**산출물:** 원본 HTML 저장 + 정책 raw CSV/JSON (원문 그대로, 가공 없이)

**완료 기준:** 50~150건 수집, 정책명·상세URL·원문 텍스트가 채워짐, 원본 파일 별도 보존

---

## STEP 2. Data Processing

**목표:** 원본 텍스트를 (A) 구조화 필드와 (B) 문서 텍스트로 분리·정제

- [ ] (A) 나이 / 지역 / 자치구 / 취업 상태 / 분야 / 신청 시작일 / 신청 종료일 → 표 형태로 추출
- [ ] (B) 정책 소개 / 지원 내용 / 상세 자격 / 제외 조건 / 신청 방법 / 제출 서류 → 문서 텍스트로 정리
- [ ] 모집 상태(모집중/상시/마감)를 신청기간 문자열 + 오늘 날짜 비교로 판별하는 로직 설계

**생각해볼 질문**
- "만 19~34세" 같은 자유 텍스트에서 최소/최대 나이를 정규식으로 어떻게 뽑을까?
- "서울 전역"과 "특정 자치구 한정"을 구분하는 기준은?

**참고 자료**
- `수업교재/문자열 조작과 정규식.pdf`, `수업교재/정규식.pdf`
- `수업교재/2. 데이터 처리를 위한 pandas.pdf`, `수업교재/5. 데이터 변형 및 치환.pdf`

**완료 기준:** 구조화 테이블 결측/이상치 확인 완료, 정책별 문서 텍스트 준비 완료

---

## STEP 3. Embedding

**목표:** 정책 문서를 벡터로 변환

**먼저 결정할 것 (직접 판단)**
스펙 문서에 "청크 1~4개"(부분별 임베딩) 아이디어와 "ChromaDB엔 정책당 문서 1개" 설명이 같이 있습니다.
- 옵션 A: 정책 하나를 청크 4개로 나눠 각각 임베딩 → 검색은 더 정밀하지만, 검색 후 같은 정책끼리 다시 묶는 로직이 필요
- 옵션 B: 정책 하나를 통짜 문서 1개로 임베딩 → 단순하지만 특정 항목(예: 신청 방법만) 매칭은 약해질 수 있음

장단점을 직접 적어보고 하나를 고른 뒤 진행하세요. (Step 4, 5 설계에 영향을 줌)

**참고 자료**
- `AI_LEC/llm_use2/day4/vectorDBEx1.py` ~ `vectorDBEx4.py`
- `AI_LEC/llm_use2/day4/Embedding & VectorDB.pdf`, `vectorDB_정리.md` (Gemini 임베딩 API 사용법 정리되어 있음)

**완료 기준:** 정책 전체에 대해 임베딩 벡터 생성 성공, 벡터 차원/개수 확인

---

## STEP 4. ChromaDB

**목표:** 임베딩 + 구조화 메타데이터를 ChromaDB에 저장

- [ ] 컬렉션 설계 (Step 3 결정에 따라 문서 단위 결정)
- [ ] 메타데이터에 나이조건/지역/자치구/취업상태/분야/신청시작일/신청종료일/모집상태 등을 넣어서, 이후 `where` 필터에 쓸 수 있게 설계

**참고 자료**
- `AI_LEC/llm_use2/day4/vectorDBEx1.py`, `vectorDBEx2.py` (필터링 관련 폴더 `chroma_filter_data` 이미 실습함)

**완료 기준:** `collection.count()`로 저장 건수 확인, 메타데이터 필터 테스트 성공

---

## STEP 5. Retrieval

**목표:** 사용자 조건 입력 → 6단계 필터링 파이프라인 구현
(지역·자치구 → 나이 → 취업상태·분야 → 모집상태 → 의미 기반 검색 → 유사도 threshold)

**참고 자료**
- `AI_LEC/llm_use2/day5/vectorDBEx5.py`, `vectorDBEx6.py` — `chroma_rag_threshold_data`(유사도 임계값으로 거르기), `chroma_chunk_data`(청크 단위 검색) 이미 실습한 내용

**생각해볼 질문**
- 필터 단계를 다 거쳤는데 남은 정책이 0개면 사용자에게 뭐라고 안내할까?
- 유사도 임계값은 어떻게 정할까? (직접 몇 개 케이스로 실험해서 값을 잡아보기)

**완료 기준:** 예시 조건 입력 시 합리적인 정책 리스트 반환, 임계값 미달 시 처리 확인

---

## STEP 6. Gemini 이용

**목표:** 검색된 정책을 근거로 자연어 응답 생성 (스펙의 출력 예시 포맷: 신청가능성 / 일치조건 / 추가확인 / 근거문서)

**참고 자료**
- `AI_LEC/llm_use/day1` — CO-STAR, RISEN 프롬프트 구조, `llm_promptEx4~8.py`
- `AI_LEC/llm_use2/day1/geminiToolEx1~4.py` — 구조화 출력(structured output) 방식 (출력 포맷이 고정된 이번 프로젝트에 유용)

**완료 기준:** 예시 사용자 입력 3개 이상에 대해 지정된 포맷으로 응답 생성 확인

---

## STEP 7. Function Calling

**목표:** Gemini가 필요할 때 검색/필터 함수를 스스로 호출하도록 연결
(예: `search_policies(지역, 나이, 분야, 취업상태)`, `get_policy_detail(policy_id)`)

**참고 자료**
- `AI_LEC/llm_use2/day2/functionCallEx1~5.py`, `day3/functionCallEx6~8.py`, `functionCallEx8.md`

**완료 기준:** 자연어 질문 → Gemini가 알맞은 함수를 호출 → 결과를 반영한 응답 확인

### 참고: 크롤링 갱신 전략 비교 (Step 5/7 설계용, 2026-08-30 논의)

크롤링(requests/bs4)은 토큰을 쓰지 않는다 — 토큰은 (1) 재수집분 재임베딩, (2) Function Calling 왕복에서
Gemini에 돌려주는 함수 결과 텍스트 양에서만 발생한다. 이 전제로 비교한 경우의 수:

| 방식 | 정확성 | 응답 속도 | 토큰 비용 | 사이트 부담 | 구현 난이도 |
|---|---|---|---|---|---|
| A. 최초 1회만 크롤링, 갱신 없음 | 낮음 | 가장 빠름 | 가장 낮음 | 최소 | 가장 쉬움 |
| B. 스케줄 기반 전체 재크롤링 (예: 매일 1회) | 중간 | 빠름 | 낮음 | 낮음 | 중간 |
| C. 쿼리 시점에 후보 정책만 실시간 재검증 (Function Calling) | 높음 | 약간 느려짐(+수초) | 약간 증가 | 최소 | 중간 |
| D. TTL 하이브리드 (B+C, `last_checked_at` 기반 캐시) | 높음 | 대부분 빠름 | 가장 효율적 | 최소 | 가장 높음 |
| E. 공지사항 게시판만 자주 모니터링 → 변경분만 타겟 재크롤링 | 중상 | 빠름 | 낮음 | 매우 낮음 | 중상 |
| F. 매 쿼리마다 전체 재크롤링 | 이론상 최고 | 가장 느림 | 파이프라인 전체 재실행이라 비현실적 | 매우 큼 | 낮음(비권장) |

**채택 방향:** C를 기본으로 하되, 정책별 `last_checked_at`을 저장해서 "오늘 이미 확인한 정책은 재검증 skip"
하는 간단한 TTL 조건만 얹는 절충안 추천 (D의 핵심 이득을 C 수준 구현 난이도로 확보).
함수 결과를 Gemini에 돌려줄 때는 원문 HTML이 아니라 "변경 여부 + 변경된 필드"만 축약해서 전달 — 토큰 절약.
E(공지사항 모니터링)는 스펙에 이미 수집 대상으로 포함되어 있으니 여유 되면 확장 기능으로 결합 가능.

**최종 확정 (2026-08-30):** 기본 검색은 자동 재검증 없이 캐시(로컬 DB) 결과 + `last_checked_at`을 그대로 보여준다.
실시간 재검증은 사용자가 요청할 때만 트리거 — 예시 문장(자연어) 또는 결과 카드의 재확인 버튼으로 유도.
이러면 평소 검색 비용/속도는 A·B 수준으로 떨어지고, 재검증(C) 비용은 사용자가 실제로 원할 때만 발생한다.

Function은 최소 2개로 나뉜다: 검색용(`search_policies` 등, 캐시만 조회) / 재검증용(`recheck_policy(policy_id)`,
해당 정책만 실시간 재크롤링 → DB에 결과 반영 → 변경분 반환). 재검증 결과를 DB에 다시 써두면, 다음 검색부터는
다른 조회에서도 갱신된 정보를 받게 되어 캐시가 사용 패턴에 따라 자연스럽게 최신화된다.

추가로 고려할 것 (직접 판단):
- `last_checked_at`을 절대 시각이 아니라 "n일 전 확인됨" 같은 상대 표현 + 신선도 배지로 보여주면 사용자가 더 직관적으로 판단 가능
- 재검증 트리거는 자연어(Function Calling 데모용)와 버튼(실사용 편의용)을 병행하는 것도 고려
- 같은 정책 반복 재검증 요청에 대한 최소 쿨다운(예: 몇 분 이내 재요청 방지)으로 사이트 부담 관리

---

## STEP 8. Streamlit

**목표:** 입력 폼(지역/나이/취업상태/관심분야) + 결과 카드 UI

**참고 자료**
- `수업교재/Streamlit_문법_가이드.pdf`

**완료 기준:** `streamlit run` 로컬 실행 성공, 입력 → 결과 화면 흐름 동작 확인

---

## 진행 방식 메모
- 한 단계씩 직접 작성 → 막히는 부분/에러를 공유 → 힌트·질문으로 같이 풀기 → 다음 단계
- 전체 코드를 한 번에 요청하기보다, 작은 단위로 시도해보고 확인받는 방식을 권장
